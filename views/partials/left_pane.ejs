<div id="left">
    <div id="new_entry">
        <a href="/">
            <h1>New Entry</h1>
        </a>
    </div>
    <div class="top_left">
        <span class="top_left_text">
            Recent Entries
        </span>
    </div>
    <div id="delete_entry">
        <h1>Delete Selected Entries</h1>
    </div>
    <ul id="entry_list">
        <% for( let entry of entries) { %>
            <li>
                <div class="entry_item" id="<%= entry._id %>">
                    <div>
                        <a href="/?id=<%= entry._id %> "><%= entry.title %></a>
                    </div>
                    <div id="date_label">
                        <%= new Intl.DateTimeFormat('en-US', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }).format(entry.date) %> 
                    </div>
                </div>
            </li>
        <% } %>
    </ul>
</div>